



{

  "scripts": {
    "build": "cd src && npm install && cd ../frontend && npm install && npm run build",
    "start": "node src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-rate-limit": "^7.5.0",
    "google-auth-library": "^9.15.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.6",
    "nanoid": "^3.3.8",
    "rate-limiter-flexible": "^5.0.5",
    "redis": "^4.7.0",
    "swagger-ui-express": "^5.0.1"
  }
}
